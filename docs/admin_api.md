# Legacy Admin API

The primary management surface is now `/admin`.

The older `/api/v1` tenant API is retained only for backward compatibility with existing scripts. New setup should use the UI to create sources, sync assets, create clients, link assets, and rotate MCP tokens.
